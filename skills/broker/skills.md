# Broker Skill
Handles order execution and position management (Zerodha Kite).

## Tools
- `place_nifty_order(symbol, quantity, side, order_type, price)`: Place a limit/market order.
- `get_positions()`: Retrieve current open positions.
- `get_holdings()`: Retrieve current stock holdings.
